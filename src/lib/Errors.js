const Errors = {
    Types: {
        KEYGUARD: 'Keyguard',
    },
    Messages: {
        CANCEL: 'CANCEL',
        KEY_ID_NOT_FOUND: 'keyId not found',
    }
}

if (errorContainer) {
    errorContainer.Errors = Errors
}
